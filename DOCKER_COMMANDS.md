# Docker Commands Cheat Sheet

Энэхүү файл нь Aero-Doc AI төслийг ажиллуулахад хэрэгтэй Docker командуудыг агуулна.

## 🚀 Үндсэн Командууд

### 1. Асаах (Start)
Бүх service-үүдийг асаана. Хэрэв image байхгүй бол татаж авна.
```bash
docker-compose up -d
```
* `-d`: Detached mode (цаана ажиллана, terminal гацахгүй)

### 2. Шинэчлэлт хийж Асаах (Rebuild & Start)
Кодонд өөрчлөлт орсон эсвэл `requirements.txt`-д сан нэмсэн бол заавал ингэж асаана.
```bash
docker-compose up -d --build
```
* `--build`: Image-ийг шинээр угсарна (таны сая асуусан команд)

### 3. Зогсоох (Stop)
Ажиллаж буй бүх service-үүдийг зогсооно.
```bash
docker-compose stop
```

### 4. Устгаад Зогсоох (Down)
Service-үүдийг зогсоож, container болон network-ийг устгана. (Data устахгүй)
```bash
docker-compose down
```

---

## 🔍 Хяналт (Monitoring)

### Logs харах (Бүгд)
```bash
docker-compose logs -f
```

### Logs харах (Нэг service)
Зөвхөн backend-ийн алдааг харах:
```bash
docker-compose logs -f backend
```
Зөвхөн database-ийг харах:
```bash
docker-compose logs -f postgres
```

### Төлөв шалгах
Ямар container-үүд ажиллаж байгааг харах:
```bash
docker-compose ps
```

---

## 🛠 Алдаа шийдвэрлэх

### Бүгдийг цэвэрлээд шинээр эхлэх
Хэрэв database эсвэл тохиргоо бүр мөсөн эвдэрвэл (Анхаар: **Бүх data устана!**):
```bash
docker-compose down -v
docker-compose up -d --build
```
* `-v`: Volumes буюу хадгалсан data-г устгана.
